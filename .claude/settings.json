{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || git commit -m 'auto: sync changes' && git push origin claude/wizardly-volta-S7fQn 2>/dev/null || true && cd /home/user/ATLAS/web && npm run build:cloudflare && npx wrangler pages deploy ../dist/cloudflare-pages --project-name atlas-site 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub & deploying to Cloudflare...",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
